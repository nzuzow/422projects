Example README for CSE422 FS14 lab2

Compilation:
   make all

Proxy Invocation:
   ./proxy

Client Invocation:
   ./client -s <server_addr> [-p <proxy_addr>]

   -s    server address, to be more precise, the remote URL. Format:
         protocol://host:port/path. This simple program only handles HTTP.
         If protocol is not specified, the program assumes the protocol
         to be http.

   -p    Optional argument. If not specified, the program requests to the
         server. If specified, the program contact the proxy and the proxy
         forward the request for the program.
         NOTE: When specifying proxy URL with port number, the format is
               host:port. example: arctic.cse.msu.edu:8080. If no port is
               specified, it is assumed to be 8080

Logs:
